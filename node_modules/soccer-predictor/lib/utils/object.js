'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

exports.iterate = iterate;
exports.map = map;
function iterate(source, fn) {
  return map(source, function (value) {
    if ((typeof value === 'undefined' ? 'undefined' : _typeof(value)) === 'object' && value instanceof Array === false) {
      return iterate(value, fn);
    }
    return fn(value);
  });
}

function map(obj, fn) {
  return Object.keys(obj).reduce(function (output, key) {
    output[key] = fn(obj[key]);
    return output;
  }, {});
}